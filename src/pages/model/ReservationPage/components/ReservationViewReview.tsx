import { Stack } from '@mui/material';
import ReservationReviewItem from './ReservationReviewItem';

const ReservationViewReview = () => {
  return (
    <Stack>
      <ReservationReviewItem />
    </Stack>
  );
};

export default ReservationViewReview;
